<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://smfarjad.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://smfarjad.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-09-08T16:32:57+00:00</updated><id>https://smfarjad.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">edgeOps: Benchmarking ML Inference at Edge</title><link href="https://smfarjad.github.io/blog/2025/edgeOps-Benchmarking-ML-Inference-at-Edge/" rel="alternate" type="text/html" title="edgeOps: Benchmarking ML Inference at Edge"/><published>2025-04-13T00:00:00+00:00</published><updated>2025-04-13T00:00:00+00:00</updated><id>https://smfarjad.github.io/blog/2025/edgeOps-Benchmarking-ML-Inference-at-Edge</id><content type="html" xml:base="https://smfarjad.github.io/blog/2025/edgeOps-Benchmarking-ML-Inference-at-Edge/"><![CDATA[<h3 id="1-introduction">1. Introduction</h3> <p>In this blog, we discuss the problem of capturing system statistics when running machine learning (ML) models on resource-constrained edge devices (aka: small platforms with tiny brains - don’t take it literally!). Ready? Let’s dive into the problem (and potential solution).</p> <p>Imagine an ML practitioner named Zeki, who loves experimenting with training ML models and deploying them on small and resource-constrained edge devices (e.g., Raspberry Pi). One day, Zeki deploys an ML model that unexpectedly consumes excessive resources, causing the device to freeze and break every running operation. In a personal setting, he can afford to power off the device and stop the model manually. But what if Zeki is working in a production system? Can he really afford the downtime caused by restarting a production device?</p> <p>It depends on the organization and the scenario, of course, but the consequences become much more serious if the system is deployed in a critical environment, such as one related to national security.</p> <p>This is where edgeOps comes into play. With edgeOps, we capture the average system statistics of the hosting edge device while running inference operations of an ML model with a testing dataset.</p> <hr/> <h3 id="2-overview-of-the-architecture">2. Overview of the Architecture</h3> <p>edgeOps consists of two main components:</p> <h4 id="21-ml-inference">2.1. ML Inference</h4> <p>This component is responsible for running a trained ML model using the provided test dataset. It varies from model to model, and the user can optimize it according to the trained model they are using. For example, a model trained for image processing might require input images of a certain size. This will require users to preprocess the test images to match that size so that the trained model can process them as designed. Similarly, a model trained for sensor data (e.g., acceleration) will require the inference component to be modified accordingly. The model-agnostic design of this inference component allows end users to try different types of ML models with edgeOps.</p> <h4 id="22-resource-monitoring">2.2. Resource Monitoring</h4> <p>This component is mainly responsible for monitoring the resources utilized by the ML model. Currently, edgeOps focuses on memory consumption, CPU utilization, and inference latency. Since each iteration of an inference model can result in different resource utilization due to background services running on the edge device, the resource monitoring component performs multiple iterations of ML inference and provides average statistics of resource consumption.</p> <p>In addition to these two components, a wrapper component (i.e., edgeOps.sh) is used to connect all components and support statistics collection. As input, edgeOps takes trained ML model(s) and test dataset(s). As output, it generates corresponding CSV and text files containing average resource utilization statistics. The following figure summarizes the overview of the edgeOps architecture.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/p3_edgeOps-480.webp 480w,/assets/img/p3_edgeOps-800.webp 800w,/assets/img/p3_edgeOps-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/p3_edgeOps.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <hr/> <h3 id="3-working">3. Working</h3> <p>For capturing resource utilization, edgeOps uses different approaches designed after accounting for various challenges (details can be found in the paper). For memory consumption, edgeOps relies on the Python memory_profiler library. For inference latency, the built-in date utility of the Linux environment is used with nanosecond precision. Additionally, the psutil library is used to capture CPU utilization.</p> <p>To ensure isolation of statistics for each iteration of ML inference, edgeOps inserts padded chunks of 30 seconds before and after each iteration. In addition to providing isolation, these padded chunks also help in measuring CPU utilization. Average CPU utilization is measured by subtracting the average CPU usage under ML inference from the average CPU usage during the padded chunks (when the ML inference is not operational). Each iteration of ML inference results in a CSV file. The following figure shows the CPU utilization from one such CSV file. The padded chunks can be observed surrounding the elevated plateau in the graph, where the plateau represents CPU utilization during ML inference.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/p3_cpu_usage-480.webp 480w,/assets/img/p3_cpu_usage-800.webp 800w,/assets/img/p3_cpu_usage-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/p3_cpu_usage.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Unlike CPU utilization, memory usage is recorded in a single text file with the average memory usage for each iteration. These output files can be analyzed to extract all three resource utilization metrics (i.e., inference latency, CPU utilization, and memory consumption).</p> <hr/> <h3 id="4-usage">4. Usage</h3> <p>How do we use edgeOps? Well, the answer is quite simple. As discussed earlier, edgeOps consists of two components, and all you need to do is modify the inference component according to your ML model and test dataset. Once the inference component is modified, open a terminal and run the main script (i.e., edgeOps.sh).</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>bash edgeOps.sh
</code></pre></div></div> <hr/> <h3 id="5-demo-with-a-trained-model">5. Demo with a Trained Model</h3> <p>Clone/download the GitHub repository for <a href="https://github.com/smfarjad/edgeOps.git">edgeOps</a>.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>git clone https://github.com/smfarjad/edgeOps.git
</code></pre></div></div> <p>Unzip (if needed) and navigate to the downloaded repository.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">cd </span>edgeOps-main
</code></pre></div></div> <p>Create an empty directory <code class="language-plaintext highlighter-rouge">models</code>.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">mkdir </span>models
</code></pre></div></div> <p>Request the model via sfarjad@unomaha.edu and move the downloaded model in <code class="language-plaintext highlighter-rouge">models</code> directory. I will share the access to download the model. However, the model can be viewed at: <a href="https://drive.google.com/drive/folders/1dscduIzHaTxE1MJOvDjATare4l4mHCqV?usp=sharing">Google Drive</a>.</p> <p>Open inference.py, update the path to the trained model in the model variable.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>model <span class="o">=</span> tf.keras.models.load_model<span class="o">(</span><span class="s2">"models/cnn_image_classification_model.h5"</span><span class="o">)</span> <span class="c"># update accordingly</span>
</code></pre></div></div> <p>Run edgeOps.sh.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>bash edgeOps.sh
</code></pre></div></div> <p>It will result in one memory profile text file that will be containing memory usage data and ten CSV files moved to a sub directory within the same repository director.</p> <hr/> <h3 id="6-conclusion">6. Conclusion</h3> <p>This blog provides a very brief overview of edgeOps, a tool for benchmarking machine learning (inference) operations at the edge. The development of edgeOps was motivated by challenges we experienced during another project, which focused on creating a secure edge computing reference architecture for structural health monitoring. edgeOps offers a scalable and easy-to-deploy solution that can be integrated into any edge computing project. However, we do not recommend using it in production systems or security-critical environments at this stage, as it is still in the early phases of development and requires further maturity.</p> <p>If you’re interested in exploring edgeOps and the reference architecture project in more detail, I recommend checking out our <a href="https://arxiv.org/abs/2503.18857">conference paper</a>. If you face any issues or would like to provide feedback, please leave a comment here or connect with me via <a href="mailto:sfarjad@unomaha.edu">email</a>.</p> <hr/> <h3 id="7-references">7. References</h3> <p>Link to GitHub Repository for edgeOps: <a href="https://github.com/smfarjad/edgeOps">https://github.com/smfarjad/edgeOps</a></p> <p><strong>Citation:</strong></p> <div class="language-bibtex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">@inproceedings</span><span class="p">{</span><span class="nl">10.1145/3696673.3723074</span><span class="p">,</span>
<span class="na">author</span> <span class="p">=</span> <span class="s">{Farjad, Sheikh Muhammad and Patllola, Sandeep Reddy and Kassa, Yonas and Grispos, George and Gandhi, Robin}</span><span class="p">,</span>
<span class="na">title</span> <span class="p">=</span> <span class="s">{Secure Edge Computing Reference Architecture for Data-driven Structural Health Monitoring: Lessons Learned from Implementation and Benchmarking}</span><span class="p">,</span>
<span class="na">year</span> <span class="p">=</span> <span class="s">{2025}</span><span class="p">,</span>
<span class="na">isbn</span> <span class="p">=</span> <span class="s">{9798400712777}</span><span class="p">,</span>
<span class="na">publisher</span> <span class="p">=</span> <span class="s">{Association for Computing Machinery}</span><span class="p">,</span>
<span class="na">address</span> <span class="p">=</span> <span class="s">{New York, NY, USA}</span><span class="p">,</span>
<span class="na">url</span> <span class="p">=</span> <span class="s">{https://doi.org/10.1145/3696673.3723074}</span><span class="p">,</span>
<span class="na">doi</span> <span class="p">=</span> <span class="s">{10.1145/3696673.3723074}</span><span class="p">,</span>
<span class="na">pages</span> <span class="p">=</span> <span class="s">{145–154}</span><span class="p">,</span>
<span class="na">numpages</span> <span class="p">=</span> <span class="s">{10}</span><span class="p">,</span>
<span class="na">keywords</span> <span class="p">=</span> <span class="s">{edge computing, structural health monitoring, machine learning}</span><span class="p">,</span>
<span class="na">location</span> <span class="p">=</span> <span class="s">{Southeast Missouri State University, Cape Girardeau, MO, USA}</span><span class="p">,</span>
<span class="na">series</span> <span class="p">=</span> <span class="s">{ACMSE 2025}</span>
<span class="p">}</span>
</code></pre></div></div>]]></content><author><name></name></author><category term="Machine Learning"/><category term="Edge Computing"/><summary type="html"><![CDATA[A supplementary tutorial focusing on edgeOps, which is a benchmarking tool to measure the resource utilization for machine learning operations in edge devices.]]></summary></entry><entry><title type="html">A Brief Tutorial on SDN using Ryu Controller</title><link href="https://smfarjad.github.io/blog/2020/A-Brief-Tutorial-on-SDN-using-Ryu-Controller/" rel="alternate" type="text/html" title="A Brief Tutorial on SDN using Ryu Controller"/><published>2020-06-01T00:00:00+00:00</published><updated>2020-06-01T00:00:00+00:00</updated><id>https://smfarjad.github.io/blog/2020/A-Brief-Tutorial-on-SDN-using-Ryu-Controller</id><content type="html" xml:base="https://smfarjad.github.io/blog/2020/A-Brief-Tutorial-on-SDN-using-Ryu-Controller/"><![CDATA[<h3 id="1-introduction">1. Introduction</h3> <p>The conventional networking paradigm consists of end devices, switches, and routers. The end devices connected in the network communicate with each other through packet transmission. The task of transmitting packets to respective destination hosts is primarily performed by the routers and switches. The routers and switches are termed as networking devices. A networking device consists of two components: a data plane for forwarding packets and a control plane for routing packets. The control plane performs a more intensive task when compared with the data plane because it has to maintain and manage routing tables, routing algorithms, etc. This is why the control plane can be considered as the brain of a networking device.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/p2_sdn1-480.webp 480w,/assets/img/p2_sdn1-800.webp 800w,/assets/img/p2_sdn1-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/p2_sdn1.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>With the expansion of the network, the number of networking devices also increases which directly renders an impact on the performance of the network. The degradation in performance is caused by the burden of managing routing information at every networking device. The innovation of software-defined networking aims to solve this degradation problem which often arises in large networks.</p> <hr/> <h3 id="2-software-defined-networking">2. Software-Defined Networking</h3> <p>Software-Defined Networking (SDN) is an architecture that centralizes the brains of all networking devices into a single component. All the networking devices are operated through this central component. The management of packet transmission is performed by the same central entity and the networking devices behave as ‘forwarding devices’, which receive and forward packets. In technical terms, SDN dissects the control planes from networking devices and centralizes them to a single component, and the networking devices are left out with only data planes that are responsible for forwarding packets.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/p2_sdn2-480.webp 480w,/assets/img/p2_sdn2-800.webp 800w,/assets/img/p2_sdn2-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/p2_sdn2.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <hr/> <h3 id="3-sdn-architecture">3. SDN Architecture</h3> <p>SDN architecture consists of three major layers:</p> <ul> <li><strong>Application Layer:</strong> It is responsible for developing and managing networking applications.</li> <li><strong>Control Layer:</strong> It is responsible for routing packets to their respective destination while applying different rules and policies.</li> <li><strong>Infrastructure Layer:</strong> It is responsible for dealing with forwarding devices or SDN switches.</li> </ul> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/p2_sdn_arch-480.webp 480w,/assets/img/p2_sdn_arch-800.webp 800w,/assets/img/p2_sdn_arch-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/p2_sdn_arch.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <hr/> <h3 id="4-working">4. Working</h3> <p>The networking applications are built and deployed on the top of the application layer and these applications communicate with Network Operating System (NOS) through Application Programming Interfaces (APIs) which are called “Northbound APIs”. NOS communicates and implements the routing rules and policies on forwarding devices via “Southbound APIs”. The end devices are connected to these forwarding devices for executing the task of communication through packets.</p> <hr/> <h3 id="5-sdn-terminologies">5. SDN Terminologies</h3> <ul style="list-style-type:disc"> <li><b>Network Operating System:</b> Network Operating System (NOS) is used for routing packets. In the SDN paradigm, NOS is called the controller because it consists of a control plane. An SDN controller is the central component where dissected intelligence of conventional networking devices is merged. There are different SDN controllers built on the stack of different programming languages. </li> <li><b>OpenFlow:</b> OpenFlow is the core protocol of SDN architecture. The controllers use OpenFlow protocol for communicating with the switches (forwarding devices). OpenFlow is not a vendor-specific protocol which infers that the controller can communicate with every switch, irrespective of the vendors. </li> <li><b>SDN Switches:</b> The SDN switches are different from conventional switches and this is why it is better to refer SDN switches as forwarding devices because SDN switches are equipped with only data planes. SDN switches can either be in hardware form or software. Open vSwitch (OVS) is the more popular virtual switch that is used in the SDN paradigm for connecting end devices. </li> </ul> <hr/> <h3 id="6-implementation">6. Implementation</h3> <h4 id="61-setting-up-environment">6.1. Setting Up Environment</h4> <p>Install Ubuntu on the base machine or in a virtual environment. For creating and managing virtual machines, I prefer to use Qemu/KVM and virt-manager application. Other virtualization alternatives like VirtualBox and VMware can also be used for the same purpose. I am using Ubuntu 18.04 at the time of writing this tutorial.</p> <h4 id="62-installation-of-dependencies">6.2. Installation of Dependencies</h4> <p>Run the following two commands before proceeding to other commands:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>apt update
<span class="nv">$ </span><span class="nb">sudo </span>apt upgrade
</code></pre></div></div> <p>Installation of prerequisites.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>apt-get <span class="nb">install </span>git gcc python-dev libffi-dev libssl-dev libxml2-dev libxslt1-dev zlib1g-dev python-pip
</code></pre></div></div> <p>Installation of Ryu.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>pip <span class="nb">install </span>ryu
<span class="nv">$ </span>ryu-manager <span class="nt">--version</span>
</code></pre></div></div> <p class="notice--info text-justify"><i class="far fa-sticky-note"></i> <strong>Troubleshooting Error:</strong> It is highly probable that <code class="language-plaintext highlighter-rouge">ryu-manager</code> command can render an error message: <code class="language-plaintext highlighter-rouge">SyntaxError: invalid syntax</code>. This error is caused by the upgraded version of tinyrpc that is not compatible with Python 2. In order to resolve this error, uninstall the current version of tinyrpc using the command <code class="language-plaintext highlighter-rouge">pip uninstall tinyrpc</code> and install the lower version using <code class="language-plaintext highlighter-rouge">pip install tinyrpc==0.8</code>.</p> <p>Installation of Open vSwitch.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>apt-get <span class="nb">install </span>openvswitch-switch
<span class="nv">$ </span>ovs-vsctl <span class="nt">--version</span>
</code></pre></div></div> <p>Installation of Wireshark.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>apt-get <span class="nb">install </span>wireshark
<span class="nv">$ </span>wireshark <span class="nt">--version</span>
</code></pre></div></div> <p>Installation of Mininet.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>apt-get <span class="nb">install </span>mininet
<span class="nv">$ </span>mn <span class="nt">--version</span>
</code></pre></div></div> <p class="notice--info text-justify"><i class="far fa-sticky-note"></i> <strong>Tip:</strong> Mininet can also be installed by cloning its <a href="https://github.com/mininet/mininet">Github Repository</a>, but using <code class="language-plaintext highlighter-rouge">apt-get</code> package for installation is more convenient.</p> <h4 id="63-running-simple-sdn-application">6.3. Running Simple SDN Application</h4> <p>Open Wireshark utility and start capturing packets. Open three terminals and run respective commands in these terminals as mentioned below.</p> <h5 id="terminal-1">Terminal 1:</h5> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>mn <span class="nt">--topo</span><span class="o">=</span>single,3 <span class="nt">--controller</span><span class="o">=</span>remote,ip<span class="o">=</span>127.0.0.1 <span class="nt">--mac</span> <span class="nt">--switch</span><span class="o">=</span>ovsk,protocols<span class="o">=</span>OpenFlow13
</code></pre></div></div> <p>The command in Terminal 1 creates a simple topology in mininet. The topology consists of a single switch and three hosts that are connected to the switch. The topology can be tested by using <code class="language-plaintext highlighter-rouge">pingall</code> command on the same terminal. At this point, the hosts are unreachable. For making the hosts communicate with each other, there must be some rules which are implemented by the controller.</p> <h5 id="terminal-2">Terminal 2:</h5> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>ryu-manager ryu.app.simple_switch_13
</code></pre></div></div> <p>The command in Terminal 2 runs a simple application of switch. The program file for <code class="language-plaintext highlighter-rouge">simple_switch_13</code> locates in <code class="language-plaintext highlighter-rouge">/usr/lib/python2.7/ryu/ryu/app/</code>directory. The <code class="language-plaintext highlighter-rouge">simple_switch_13</code> program sets rules and policies in ryu controller, which are necessary for routing packets in a fashion similar to that of L2 Switch of conventional networking. Switch to Terminal 1 and run <code class="language-plaintext highlighter-rouge">pingall</code> command again. The hosts will be able to communicate with each other this time. A simple SDN infrastructure has been implemented at this point!</p> <h5 id="terminal-3">Terminal 3:</h5> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>ovs-vsctl show
<span class="nv">$ </span><span class="nb">sudo </span>ovs-ofctl <span class="nt">-O</span> OpenFlow13 dump-flows s1
</code></pre></div></div> <p>The commands in Terminal 3 show the brief configuration and flow table of Open vSwitch. These commands are used for inspecting the behavior and statistics of SDN switches upon receiving packets from the hosts.</p> <p>Stop capturing packets on Wireshark utility. Type <code class="language-plaintext highlighter-rouge">openflow_v4</code> in Wireshark Filter for filtering out OpenFlow (version 4) packets.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/p2_wireshark-480.webp 480w,/assets/img/p2_wireshark-800.webp 800w,/assets/img/p2_wireshark-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/p2_wireshark.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <hr/> <h3 id="7-conclusion">7. Conclusion</h3> <p>In this tutorial, I tried my best to provide readers with the necessary knowledge that is required for deploying SDN infrastructure using mininet and ryu controller. The tutorial was written to be brief and this is why the excessive details are not mentioned in the tutorial. <a href="http://mininet.org/walkthrough/">Mininet Walkthrough</a> already provides rich resources for developing topologies and performing other relevant operations in mininet. The explanation of working modules and ryu applications is covered in my next blog on this site. In case of any issue or providing your feedback, please drop a comment here or connect with me via <a href="mailto:smfarjad@outlook.com">email</a>.</p> <hr/> <h3 id="8-references">8. References</h3> <ul> <li><a href="http://mininet.org/">Mininet</a></li> <li><a href="https://ryu-sdn.org/">Ryu SDN Framework</a></li> <li><a href="https://www.openvswitch.org/">Open vSwitch</a></li> <li><a href="https://www.wireshark.org/">Wireshark</a></li> </ul>]]></content><author><name></name></author><category term="Software-Defined Networking"/><category term="Ryu Controller"/><category term="Open vSwitch"/><summary type="html"><![CDATA[A descriptive tutorial about the development and deployment of the SDN paradigm in Linux environment using Mininet and Ryu controller.]]></summary></entry><entry><title type="html">Tutorial on Deploying Kali-Ubuntu MPI Cluster</title><link href="https://smfarjad.github.io/blog/2020/Tutorial-on-Deploying-Kali-Ubuntu-MPI-Cluster/" rel="alternate" type="text/html" title="Tutorial on Deploying Kali-Ubuntu MPI Cluster"/><published>2020-06-01T00:00:00+00:00</published><updated>2020-06-01T00:00:00+00:00</updated><id>https://smfarjad.github.io/blog/2020/Tutorial-on-Deploying-Kali-Ubuntu-MPI-Cluster</id><content type="html" xml:base="https://smfarjad.github.io/blog/2020/Tutorial-on-Deploying-Kali-Ubuntu-MPI-Cluster/"><![CDATA[<h3 id="1-introduction">1. Introduction</h3> <p>In this project, we aim to develop a cluster consisting of Kali Linux and Ubuntu. This brief tutorial is using Kali and Ubuntu, but any variant of Linux can be used with minimal change. It is also worth mentioning that the cluster implemented in this project consists of a base machine and a virtual machine. In the base machine, Kali Linux was installed as a base operating system. In the virtual machine, Ubuntu was installed. I opted for this environment for exhibiting an efficient way of dealing with the cluster computing paradigm while being on a single machine.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/p1_mpi_1-480.webp 480w,/assets/img/p1_mpi_1-800.webp 800w,/assets/img/p1_mpi_1-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/p1_mpi_1.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>The steps mentioned in this tutorial can be followed for deploying the cluster in LAN environment also. It depends on the requirement of the user. For creating a virtual machine, Qemu/KVM was used in this tutorial but readers can also use VirtualBox or VMware.</p> <hr/> <h3 id="2-installation-of-core-dependencies">2. Installation of Core Dependencies</h3> <p>For developing a cluster computing environment, MPICH is to be installed on machines. MPICH is an implementation of Message Passing Interface (MPI) that is a portable message-passing standard designed for dealing with different parallel computing architectures. The following steps should be followed for installing MPICH on both machines (i.e. Base Machine and Virtual Machine):</p> <p>Download the latest version of MPICH from <a href="http://www.mpich.org/downloads/">here</a>.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/p1_mpi_2-480.webp 480w,/assets/img/p1_mpi_2-800.webp 800w,/assets/img/p1_mpi_2-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/p1_mpi_2.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Un-compress and navigate to downloaded directory.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">tar</span> –xzf mpich-3.3.2.tar.gz
<span class="nv">$ </span><span class="nb">cd </span>mpich-3.3.2
</code></pre></div></div> <p>Configure the installation.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>./configure <span class="nt">--disable-fortran</span>
</code></pre></div></div> <p class="notice--info text-justify"><i class="far fa-sticky-note"></i> <strong>Troubleshooting Error:</strong> The <code class="language-plaintext highlighter-rouge">disable-fortran</code> option disables Fortran-related packages because mpich comes in C, C++, and Fortran implementation.</p> <p>After configuration, build and install mpich.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>make<span class="p">;</span> <span class="nb">sudo </span>make <span class="nb">install</span>
</code></pre></div></div> <p>Check the installation.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>mpiexec <span class="nt">--version</span>
</code></pre></div></div> <hr/> <h3 id="3-configuring-ip-hostname-mapping">3. Configuring IP-Hostname Mapping</h3> <p>The machines existing on the network can be addressed using their hostnames. Run <code class="language-plaintext highlighter-rouge">ifconfig</code> or <code class="language-plaintext highlighter-rouge">ip addr</code> command on both machines for determining their respective IP addresses. In order to map their IP addresses to the hostnames, some entries must be appended in <code class="language-plaintext highlighter-rouge">/etc/hosts</code> file of server and client machine.</p> <p>On server,</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">cat</span> /etc/hosts
127.0.0.1	localhost

192.168.56.102 master
192.168.56.104 slave

</code></pre></div></div> <p>On client,</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">cat</span> /etc/hosts
127.0.0.1	localhost

192.168.56.102 master
192.168.56.104 slave

</code></pre></div></div> <hr/> <h3 id="4-creating-dedicated-user-accounts">4. Creating Dedicated User Accounts</h3> <p>For maintaining the simplicity and clear workflow, create separate user accounts for cluster paradigm on both machines:</p> <p>On Server Machine</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>adduser fserver
</code></pre></div></div> <p>On Client Machine</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>adduser fclient
</code></pre></div></div> <p>Separate user accounts different from the conventional user accounts will help in keeping the environment different and safe.</p> <hr/> <h3 id="5-ssh-configuration">5. SSH Configuration</h3> <p>Install openssh-server on both machines:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>apt <span class="nb">install </span>openssh-server
</code></pre></div></div> <p>The SSH service must be active for creating the operational SSH tunnel between two machines. The status of SSH service can be checked by entering the following command:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>systemctl status ssh
</code></pre></div></div> <p>If the status is inactive then SSH service can be enabled by using the command:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>systemctl restart ssh
</code></pre></div></div> <p>Once the SSH service becomes active on both machines, an ssh session can be initiated from the server machine for creating an ssh tunnel to the client machine.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>ssh fclient@ubuntu
<span class="nv">$ </span>ssh fclient@192.168.56.104
</code></pre></div></div> <p>192.168.56.104 is the IP address of the client machine (slave).</p> <hr/> <h3 id="6-nfs-configuration">6. NFS Configuration</h3> <p>Network File System (NFS) is a client-server application, which provides the feature of a shared directory that can be accessed by different network users. NFS uses the Virtual File System (VFS) interface based on TCP/IP.</p> <p>In this project, NFS is used for creating a shared directory. This shared directory is to be used by clients for storing and accessing data. The server machine, base machine in our case, is used for installing NFS-server, while the NFS-client application is installed in every client machine. The installation and configuration of NFS are further elaborated be in the following paragraphs:</p> <h4 id="61-server-machine-base-machine--kali-linux">6.1. Server Machine (Base Machine – Kali Linux)</h4> <p>Install the NFS-server</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>apt-get <span class="nb">install </span>nfs-kernel-server
</code></pre></div></div> <p>Create the shared directory (The name used by the directory in server machine will be used again for creating similar directory in client machine).</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">mkdir </span>cloud
</code></pre></div></div> <p>For exporting the shared directory, append the line <code class="language-plaintext highlighter-rouge">/home/fserver/cloud *(rw,sync,no_root_squash,no_subtree_check</code> in <code class="language-plaintext highlighter-rouge">/etc/exports</code>.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span> <span class="nb">cat</span> /etc/exports
/home/mpiuser/cloud <span class="k">*</span><span class="o">(</span>rw,sync,no_root_squash,no_subtree_check<span class="o">)</span>
</code></pre></div></div> <p>After making any change in /etc/exports, following command is run:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>exportfs –a
<span class="nv">$ </span><span class="nb">sudo </span>systemctl restart nfs-kernel-server
</code></pre></div></div> <p>The status of nfs-server can be checked by the following command:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>systemctl status nfs-kernel-server
</code></pre></div></div> <h4 id="62-client-machine-virtual-machine--ubuntu">6.2 Client Machine (Virtual Machine – Ubuntu)</h4> <p>Install the NFS-client</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>apt <span class="nb">install </span>nfs-common
</code></pre></div></div> <p>Create the same directory in client machine which is to be shared via NFS:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">mkdir </span>cloud
</code></pre></div></div> <p>Mount the shared directory</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>mount <span class="nt">-t</span> nfs master:/home/mpiuser/cloud ~/cloud
</code></pre></div></div> <p>The status of mounted directory can be checked by using following command:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">df</span> <span class="nt">-h</span>
</code></pre></div></div> <p>Every time system reboots, the shared directory is to be mounted. To avoid the hassle of mounting directory again and again, this task of mounting can be automated and the shared directory will be automatically mounted on every system reboot operation.</p> <p>Append the line “master:/home/mpiuser/cloud /home/mpiuser/cloud nfs“ in /etc/fstab file.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">cat</span> /etc/fstab
<span class="c">#MPI CLUSTER SETUP</span>
master:/home/mpiuser/cloud /home/mpiuser/cloud nfs
</code></pre></div></div> <p>The status of nfs-client can be checked by the following command:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>systemctl status nfs-common
</code></pre></div></div> <hr/> <h3 id="7-testing-the-cluster">7. Testing the cluster</h3> <p class="notice--info text-justify"><i class="far fa-sticky-note"></i> Download the program by cloning the repository <code class="language-plaintext highlighter-rouge">https://github.com/smfarjad/Kali-Ubuntu-MPI-Cluster</code> and follow the instructions mentioned in README.</p> <p>To run the program only on the same master node</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>mpirun <span class="nt">-np</span> 10 <span class="nt">--hosts</span> master ./hello_MPI
</code></pre></div></div> <p>To run the program on master and slave nodes</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>mpirun <span class="nt">-np</span> 10 <span class="nt">--hosts</span> master,slave ./hello_MPI
</code></pre></div></div> <hr/> <h3 id="8-conclusion">8. Conclusion</h3> <p>The tutorial summarizes all the necessary details for implementing a cluster using Kali Linux and Ubuntu. The readers can follow the same instructions for setting up a cluster on the other distributions of Linux. In case of any discrepancy regarding the tutorial, feel free to comment below or approach me via <a href="mailto:smfarjad@outlook.com">email</a>.</p> <hr/> <h3 id="9-references">9. References</h3> <ul> <li><a href="https://en.wikipedia.org/wiki/Message_Passing_Interface">Message Passing Interface</a></li> <li><a href="https://www.mpich.org/">MPICH</a></li> </ul>]]></content><author><name></name></author><category term="MPI"/><category term="Cluster"/><category term="Linux"/><summary type="html"><![CDATA[How to deploy MPI cluster using Kali Linux and Ubuntu?]]></summary></entry></feed>